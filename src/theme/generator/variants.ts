export function generateThemeVariants() {
  return {
    // Стандартная тема
    standard: generateStandardTheme(),
  }
}

function generateStandardTheme() {
  // Возвращаем текущую тему как стандарт
  return {} // Будет заменено на основной генератор
}
