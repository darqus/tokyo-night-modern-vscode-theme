# ⚙️ Конфигурация Tokyo Modern

## 🎛️ Обзор

Tokyo Modern предоставляет гибкую систему конфигурации, позволяющую настраивать различные аспекты темы под индивидуальные предпочтения разработчиков. Конфигурация реализована через централизованный модуль и поддерживает как статические, так и потенциально динамические настройки.

## 📁 Структура конфигурации

Конфигурация темы распределена между несколькими файлами в директории `src/theme/config/`:

### Основные параметры темы

- `config.ts` - основные параметры темы (имя, тип, описание)
- `constants.ts` - централизованные константы темы (COLOR_GENERATION, CONTRAST, PERFORMANCE, VALIDATION)
- `color-config-dsl.ts` - DSL для конфигурации цветов с типами и builder-ами
- `unified-generator.ts` - унифицированный генератор темы на основе конфигурации

### Цветовая палитра

- `basePalette` - базовая цветовая палитра в `src/theme/palette/base.ts`
- Каждый цвет имеет 4 варианта: `dark`, `main`, `light`, `bright`
- Группировка по семантическим категориям (background, foreground, blue, green и т.д.)

## 🎨 Настройка цветовой палитры

### Структура палитры

```typescript
interface Palette {
  background: {
    dark: string,   // Темный фон
    main: string,   // Основной фон
    light: string,  // Светлый фон
    bright: string  // Яркий фон
  },
  foreground: {
    dark: string,   // Темный передний план
    main: string,   // Основной передний план
    light: string,  // Светлый передний план
    bright: string  // Яркий передний план
  },
  // ... другие цвета: blue, green, cyan, teal, purple, orange, yellow, red, pink, neutral
  ui: {
    white: string,
    black: string,
    badge: string   // Цвет значков
  }
}
```

### Изменение цветов

1. Откройте файл `src/theme/palette/base.ts`
2. Найдите нужный цвет и измените его HEX-код
3. Пересоберите тему с помощью `pnpm run build`

## 🔧 Система генерации токенов

### UI токены

- Генерируются из палитры для элементов интерфейса VS Code
- Находятся в `src/theme/generator/ui/`
- Каждый элемент интерфейса (редактор, панели, вкладки и т.д.) имеет отдельный модуль
- Используют `ColorRuleBuilder` для упрощения создания цветовых правил

### Синтаксические токены

- Определяют цвета для синтаксической подсветки
- Находятся в `src/theme/generator/tokens/`
- Категории: basic, code, comments, css, markdown, markup, modern
- Используют утилиты из `token-helpers.ts` для преобразования правил

### Семантические токены

- Обеспечивают расширенную подсветку для конкретных языков и фреймворков
- Находятся в `src/theme/generator/semantic/`
- Поддерживают React, Vue, Rust, Go, Python, GraphQL и другие технологии

## 🛠️ Новые утилиты и подходы

### ColorRuleBuilder

Новый builder для упрощения создания цветовых правил:

```typescript
import { colorRules, c } from '../utils/color-builder.js'

export function generateCoreColors(): Record<string, string> {
  return colorRules()
    .add('foreground', c.fg.light)
    .add('descriptionForeground', c.fg.main)
    .addGroup('badge', {
      background: c.ui.badge,
      foreground: c.ui.white,
    })
    .build()
}
```

### Семантические утилиты цвета

Утилиты с понятными именами для часто используемых операций:

- `OPACITY` - константы прозрачности (VERY_SUBTLE, SUBTLE, LIGHT, MEDIUM, STRONG)
- `ADJUST` - константы изменения яркости (TINY, SLIGHT, SMALL, LIGHT, MEDIUM, STRONG)
- `MIX_RATIO` - константы для смешивания цветов (MOSTLY_FIRST, BALANCED, MOSTLY_SECOND)
- Функции: `subtleHighlight`, `mediumHighlight`, `hoverState`, `activeState`, `disabledState` и другие

### DSL для конфигурации

DSL для описания конфигурации цветов с типобезопасностью:

- `UIColorConfig`, `TokenColorConfig`, `SemanticTokenConfig` - типы конфигурации
- `UIConfigBuilder` - builder для создания UI конфигурации
- `createToken`, `semanticConfig` - помощники для создания токенов

## 🧪 Валидация и проверки

### Проверка контраста

- Автоматическая проверка соответствия WCAG AA
- Используются `CONTRAST_THRESHOLDS` из конфигурации
- Проверка критичных пар цветов

### Валидация структуры

- Проверка корректности HEX-кодов
- Проверка структуры темы
- Проверка наличия обязательных токенов

## 📦 Параметры сборки

### package.json

- `name` - имя расширения
- `displayName` - отображаемое имя
- `description` - описание темы
- `version` - версия темы
- `engines.vscode` - минимальная версия VS Code
- `categories` - категории расширения
- `keywords` - ключевые слова для поиска

### Скрипты сборки

- `build` - генерация темы
- `build:vsix` - создание VSIX пакета
- `test` - запуск тестов
- `test:coverage` - запуск тестов с покрытием
- `lint` - проверка кода
- `format` - форматирование кода

## 🚀 Параметры публикации

### VS Code Marketplace

- Иконка: `icon.png`
- Описание: `README.md`
- Лицензия: `LICENSE`
- Файлы темы: `themes/*.json`

### Оптимизация VSIX

- Исключены: `src/`, `test/`, `docs/`, `scripts/`
- Включены только необходимые файлы для работы темы
- Размер пакета: ~34 KB

## 🔐 Безопасность

### Безопасное выполнение

- Используется `safe-exec.ts` для фильтрации разрешенных команд
- Санитизация вывода с помощью `logger.ts`
- Проверка валидности всех конфигурационных значений

## 📊 Метрики качества

- Дублирование кода: 0%
- Хардкод: 0
- Неиспользуемый код: 0
- Покрытие тестами: 82 теста
- Типобезопасность: 100%
- WCAG compliance: 98%

## 🔄 Динамические настройки (в будущем)

Планируется реализация:

- Светлой версии темы
- Возможности выбора акцентных цветов
- Настройки интенсивности цветов
- Тематических вариантов (cool, warm, neutral)
