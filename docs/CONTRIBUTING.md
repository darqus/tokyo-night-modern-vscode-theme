# Руководство по разработке (CONTRIBUTING)

Дата: 2025-11-04

Этот документ описывает локальную разработку, тестирование и публикацию темы «Tokyo Modern» для VS Code, а также минимизацию поставки (исключение dev-папок из ветки main и VSIX).

Исключаемые каталоги (dev-only): `src/`, `test/`, `tests/`, `docs/`, `scripts/`.

---

## 1) Требования

- Node.js 18+
- PNPM (предпочтительно) или npm
- Git
- VS Code

Проверка окружения:

```bash
node -v
pnpm -v  # или npm -v
```

## 2) Установка зависимостей

```bash
pnpm install
# или
npm ci
```

## 3) Структура проекта

- `themes/` — готовые JSON-файлы темы (попадают в VSIX и main)
- `icon.png`, `README.md`, `LICENSE`, `package.json` — необходимые файлы для Marketplace
- `src/` — исходники генератора (исключены)
- `scripts/` — скрипты сборки/релиза (исключены)
- `docs/` — внутренняя документация для разработки (исключена)
- `test/`, `tests/` — тесты (исключены)

## 4) Сборка темы

```bash
pnpm run build
```

Результат: `themes/tokyo-modern-color-theme.json`. Этот файл коммитится в main.

Быстрая проверка в VS Code:

- Откройте проект и нажмите F5 (Run Extension), или
- Соберите VSIX и установите локально:

```bash
pnpm run build:vsix
code --install-extension ./tokyo-modern-*.vsix --force
```

## 5) Тестирование и качество кода

```bash
pnpm run test
pnpm run test:coverage
pnpm run lint
pnpm run format
```

Примечание: убедитесь, что перечисленные скрипты определены в `package.json`. При расхождениях синхронизируйте скрипты и документацию.

## 🎨 Как изменить цвета

Если вы хотите изменить цветовую схему, выполните следующие шаги:

1. **Откройте файл палитры:**
    `src/theme/palette/base.ts`

2. **Измените HEX-коды:**
    Найдите нужный цвет и измените его HEX-код. Например, чтобы изменить основной синий цвет:

    ```typescript
    // src/theme/palette/base.ts
    // ...
    blue: {
      dark: '#3d59a1',
      main: '#7aa2f7', // <-- Измените этот цвет
      light: '#7dcfff',
      bright: '#b4f9f8',
    },
    // ...
    ```

3. **Пересоберите тему:**
    Выполните команду в терминале:

    ```bash
    pnpm run build
    ```

    Это обновит файл `themes/tokyo-modern-color-theme.json`.

4. **Проверьте изменения:**
    Нажмите `F5`, чтобы запустить VS Code в режиме разработки с вашей обновленной темой.

## 6) Архитектура проекта

Tokyo Modern использует модульную архитектуру:

- `src/theme/config.ts` — централизованная конфигурация
- `src/theme/config/` — дополнительные файлы конфигурации (constants, DSL, unified-generator)
- `src/theme/palette/` — цветовая палитра
- `src/theme/generator/` — генераторы темы (UI, токены, семантические)
- `src/theme/utils/` — утилиты (цвета, контраст, валидация, кэширование, builder-ы)
- `src/theme/types/` — типы TypeScript

Для подробного описания архитектуры см. [ARCHITECTURE.md](./ARCHITECTURE.md).

## 14) Новые утилиты и подходы

### ColorRuleBuilder

Для упрощения создания цветовых правил был добавлен builder-паттерн:

```typescript
import { colorRules, c } from '../utils/color-builder.js'

export function generateCoreColors(): Record<string, string> {
  return colorRules()
    .add('foreground', c.fg.light)
    .add('descriptionForeground', c.fg.main)
    .addGroup('badge', {
      background: c.ui.badge,
      foreground: c.ui.white,
    })
    .build()
}
```

### Семантические утилиты цвета

Новые утилиты с понятными именами для часто используемых операций:

```typescript
// Подсветка
export const subtleHighlight = (color: string) => alpha(color, OPACITY.LIGHT)
export const mediumHighlight = (color: string) => alpha(color, OPACITY.MEDIUM)
export const strongHighlight = (color: string) => alpha(color, OPACITY.STRONG)

// Состояния элементов
export const hoverState = (base: string) => lighten(base, ADJUST.LIGHT)
export const activeState = (base: string) => lighten(base, ADJUST.MEDIUM)
export const disabledState = (base: string) => alpha(base, OPACITY.STRONG)
```

### DSL для конфигурации

Новый DSL для описания конфигурации цветов с типобезопасностью:

```typescript
export interface UIColorConfig {
  rules?: Record<string, ColorValue>
  groups?: Record<string, Record<string, ColorValue>>
  multiple?: Array<[string[], ColorValue]>
}
```

### Утилиты для работы с токенами

Новые утилиты для преобразования правил builder в токены:

```typescript
export function convertRulesToTokens(
  rules: Record<string, any>,
  tokenDefinitions: Record<string, (value: any, scopeStr: string) => TokenColor | null>
): TokenColor[]
```

## 7) Тестирование

Проект имеет 82 unit-теста, покрывающих все критичные утилиты:

- Color Utils: 10 тестов
- RGB Utils: 9 тестов
- Contrast Utils: 7 тестов
- Validation Utils: 7 тестов
- Palette Generation: 4 теста

Для подробного описания тестирования см. [TESTING.md](./TESTING.md).

## 8) Конфигурация

Конфигурация темы централизована в `src/theme/config.ts` и включает:

- Основные параметры темы (имя, тип)
- Пороговые значения контраста для WCAG compliance
- Цветовую палитру в `src/theme/palette/base.ts`

Для подробного описания конфигурации см. [CONFIGURATION.md](./CONFIGURATION.md).

## 9) Политика исключений и минимальной поставки

- `.vscodeignore` исключает dev-файлы из VSIX.
- `.gitignore` исключает dev-файлы из индекса Git (и, следовательно, из main).

Ключевые исключения (должны присутствовать в обоих файлах):

```
src/**
test/**
tests/**
docs/**
scripts/**
```

Если dev-директории уже попали в индекс:

```bash
git rm -r --cached src/ test/ tests/ docs/ scripts/
git commit -m "chore: remove dev-only directories from tracking"
```

## 10) Политика ветвления и PR

- Работайте в фиче-ветках: `git checkout -b feature/your-change`
- В PR в main допускаются изменения только пользовательских артефактов:
  - `themes/*.json`, `package.json`, `README.md`, `LICENSE`, `icon.png`
- Запрещено добавлять в main: `src/`, `tests/`, `docs/`, `scripts/`
- Чеклист перед PR:
- [ ] Сгенерирован `themes/*.json` (если требуется)
- [ ] Прошли тесты/линт (локально)
- [ ] В PR нет файлов из dev-директорий
- [ ] Проверен состав VSIX: `npx @vscode/vsce ls`

## 11) Релиз и публикация

- Обновите версию в `package.json`
- Сборка темы: `pnpm run build`
- Формирование пакета:

```bash
pnpm run build:vsix
npx @vscode/vsce ls
npx @vscode/vsce package
```

- Публикация (при наличии токена):

```bash
npx @vscode/vsce publish
```

## 12) Планы улучшений

Проект находится в статусе PRODUCTION READY с оценкой качества 9/10. Основные планы улучшений включают:

- Реализацию семантических токенов
- Создание светлой версии темы
- Добавление динамических настроек

Для подробного плана улучшений см. [IMPROVEMENT_PLANS.md](./IMPROVEMENT_PLANS.md).

## 13) Полезные команды

- Проверка состава VSIX: `npx @vscode/vsce ls`
- Удаление установленного расширения: `code --uninstall-extension tokyo-modern`
- Переустановка локального пакета: `code --install-extension ./tokyo-modern-*.vsix --force`

---
Документ поддерживает стратегию: dev-папки используются локально, итоговые артефакты — в `themes/` и поставке. Это гарантирует, что пользователи получают только минимально необходимый набор файлов.

Итог: конфигурация и документация соответствуют политике исключений. Для полной уверенности запустите две команды проверки `vsce ls`, но по текущему содержимому файлов правил исключения — всё настроено корректно.
