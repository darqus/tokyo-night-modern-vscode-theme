# 📐 Архитектура Tokyo Modern

## 🎯 Обзор

Tokyo Modern - это тема для VS Code с модульной архитектурой, обеспечивающей гибкость, масштабируемость и высокое качество кода. Архитектура спроектирована с использованием принципов чистого кода и модульного программирования.

## 🏗️ Структура проекта

```
src/theme/
├── config.ts              # Конфигурация темы
├── index.ts               # Точка входа
├── generator/             # Генераторы темы
│   ├── index.ts           # Основной генератор
│   ├── semantic/          # Генераторы семантических токенов
│   │   ├── functions.ts  # Токены функций
│   │   ├── variables.ts  # Токены переменных
│   │   └── modern-frameworks.ts # Токены фреймворков
│   ├── tokens/            # Генераторы токенов синтаксиса
│   │   ├── basic.ts      # Базовые токены
│   │   ├── code.ts       # Токены кода
│   │   ├── comments.ts   # Токены комментариев
│   │   ├── css.ts        # Токены CSS
│   │   ├── markdown.ts   # Токены Markdown
│   │   ├── markup.ts     # Токены разметки
│   │   └── modern.ts     # Современные токены
│   └── ui/                # Генераторы UI токенов
│       ├── buttons.ts    # Токены кнопок
│       ├── core.ts       # Основные токены UI
│       ├── diff.ts       # Токены diff
│       ├── editor.ts     # Токены редактора
│       ├── git.ts        # Токены Git
│       ├── inputs.ts     # Токены полей ввода
│       ├── lists.ts      # Токены списков
│       ├── misc.ts       # Разные токены
│       ├── panels.ts     # Токены панелей
│       ├── sidebar.ts    # Токены боковой панели
│       ├── tabs.ts       # Токены вкладок
│       └── terminal.ts   # Токены терминала
├── palette/
│   ├── base.ts           # Базовая цветовая палитра
│   ├── generated.ts      # Генерация палитры
│   └── index.ts          # Экспорт палитры
├── types/
│   ├── index.ts          # Экспорт типов
│   ├── palette.ts        # Типы палитры
│   └── theme.ts          # Типы темы
└── utils/
    ├── cache.ts          # Кэширование
    ├── color.ts          # Утилиты цветов
    ├── color-generator.ts # Генераторы цветовых палитр
    ├── contrast.ts       # Утилиты контраста
    ├── logger.ts         # Логирование
    ├── rgb.ts            # Утилиты RGB
    ├── safe-exec.ts      # Безопасное выполнение
    ├── semantic-tokens.ts # Утилиты семантических токенов
    └── validation.ts     # Валидация
```

## 🧱 Модульная архитектура

### 1. Генераторы

Центральным элементом архитектуры является модульная система генераторов:

- **UI генераторы** - отвечают за цвета элементов интерфейса VS Code
- **Токен генераторы** - отвечают за синтаксическую подсветку
- **Семантические генераторы** - обеспечивают расширенную подсветку для конкретных языков и фреймворков

### 2. Палитра

Цветовая палитра централизована и структурирована:

- Каждый цвет имеет 4 варианта: `dark`, `main`, `light`, `bright`
- Группировка по семантическим категориям (background, foreground, blue, green и т.д.)
- Используется для генерации всех цветов темы

### 3. Утилиты

Набор переиспользуемых утилит:

- **color.ts** - функции работы с цветами (lighten, darken, mix, alpha)
- **rgb.ts** - конвертация между цветовыми форматами
- **contrast.ts** - проверка контраста и соответствия WCAG
- **validation.ts** - валидация темы и цветов
- **cache.ts** - кэширование для оптимизации
- **semantic-tokens.ts** - работа с семантическими токенами

## 🔄 Процесс генерации темы

1. **Конфигурация**: `THEME_CONFIG` определяет основные параметры темы
2. **Палитра**: `basePalette` содержит все базовые цвета
3. **Генераторы**: Функции в `generator/` используют палитру для создания специфичных токенов
4. **Сборка**: Все части объединяются в один объект темы

## 🧪 Тестирование

- 82 unit-теста покрывают все критичные утилиты
- Используется Vitest с coverage reporting
- Проверяются граничные случаи и корректность вычислений

## 🔧 Ключевые особенности

### Модульность

- Каждый компонент имеет одну ответственность
- Легко добавлять новые токены или изменять существующие
- Возможность переиспользования утилит

### Типобезопасность

- Полностью на TypeScript
- Строгая типизация всех компонентов
- Определены типы для палитры и темы

### Качество кода

- Нет дублирования (0%)
- Нет хардкода (0)
- Нет неиспользуемого кода (0)
- Полная документация (JSDoc)

## 🚀 Готовность к продакшену

- ✅ Все критичные требования выполнены
- ✅ Высокое покрытие тестами
- ✅ Соблюдение стандартов WCAG
- ✅ Оптимизированный VSIX пакет
- ✅ Полная документация
