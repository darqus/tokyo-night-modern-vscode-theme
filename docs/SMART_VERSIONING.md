# Умная система автоматического версионирования

## 🤖 Обзор

Реализована умная система релизов, которая автоматически анализирует conventional commits и определяет правильный тип версии (patch/minor/major) без ручного указания.

## ✨ Ключевые возможности

### 🔍 Автоматический анализ коммитов

- **Анализ типов**: `feat`, `fix`, `perf`, `docs`, `refactor`, `style`
- **Обнаружение Breaking Changes**: `feat!:` или `BREAKING CHANGE:`
- **Умное определение версии**: автоматический выбор patch/minor/major

### 📊 Детальная отчетность

- Показ статистики изменений
- Краткое содержание изменений с эмодзи
- Предварительный просмотр новой версии
- Предупреждения о breaking changes

### 🛠️ Гибкость использования

- **Автоматический режим**: `npm run release` (рекомендуется)
- **Принудительные релизы**: `npm run release:patch/minor/major`
- **Dry-run режим**: для предварительного просмотра
- **Force режим**: игнорирование проверок git

## 📋 Команды

### Основные команды

```bash
# Автоматическое определение типа релиза (РЕКОМЕНДУЕТСЯ)
npm run release

# Предварительный просмотр без изменений
npm run release:dry

# Анализ коммитов без релиза
npm run version:analyze
```

### Принудительные релизы

```bash
npm run release:patch    # Принудительный patch (0.0.X)
npm run release:minor    # Принудительный minor (0.X.0)
npm run release:major    # Принудительный major (X.0.0)
```

### Prerelease версии

```bash
npm run release:alpha    # Альфа версия (1.0.0-alpha.0)
npm run release:beta     # Бета версия (1.0.0-beta.0)
npm run release:rc       # Release candidate (1.0.0-rc.0)
```

### Специальные команды

```bash
npm run release:first    # Первый релиз проекта
npm run release -- --force  # Релиз игнорируя git проверки
```

## 🧠 Логика определения версий

### Автоматические правила

| Тип коммита | Результат | Пример |
|-------------|-----------|---------|
| `fix:` | **PATCH** (0.0.X) | 1.2.3 → 1.2.4 |
| `perf:` | **PATCH** (0.0.X) | 1.2.3 → 1.2.4 |
| `feat:` | **MINOR** (0.X.0) | 1.2.3 → 1.3.0 |
| `feat!:` или `BREAKING CHANGE:` | **MAJOR** (X.0.0) | 1.2.3 → 2.0.0 |

### Приоритет определения

1. **MAJOR**: если есть любой breaking change
2. **MINOR**: если есть `feat:` (без breaking changes)
3. **PATCH**: если есть только `fix:` или `perf:`

## 🔍 Примеры анализа

### Сценарий 1: Только исправления

```
Коммиты:
- fix(theme): исправить контрастность кнопок
- fix(build): исправить сборку на Windows

Результат: PATCH версия (1.2.3 → 1.2.4)
```

### Сценарий 2: Новые функции

```
Коммиты:
- feat(theme): добавить темный режим
- fix(ui): исправить отступы

Результат: MINOR версия (1.2.3 → 1.3.0)
```

### Сценарий 3: Breaking changes

```
Коммиты:
- feat(api)!: новый формат конфигурации
- fix(theme): исправить цвета

Результат: MAJOR версия (1.2.3 → 2.0.0)
```

## 📊 Вывод системы

### Пример автоматического анализа

```
🤖 Запускаем умную систему релизов...
🔍 Анализ коммитов завершен:
   📝 Коммитов для анализа: 5
   ✨ Features: ✅
   🐛 Fixes: ✅
   💥 Breaking: ❌
   ⚡ Performance: ❌
📈 Рекомендованный тип релиза: MINOR
📋 Краткое содержание изменений:
   • ✨ добавить темный режим
   • 🐛 исправить контрастность кнопок
   • 📚 обновить документацию
📊 Версия: 1.2.3 → 1.3.0
```

### Предупреждения о Breaking Changes

```
📊 Информация о релизе:
   🏷️  Версия: 2.0.0
   📝 Изменений: 8
   💥 ВНИМАНИЕ: Есть breaking changes!
```

## 🛡️ Проверки безопасности

### Автоматические проверки

1. **Git статус**: нет незакоммиченных изменений
2. **Тесты**: все тесты должны пройти
3. **Сборка**: успешная сборка всех вариантов
4. **Валидация**: темы проходят проверку качества

### Обход проверок

```bash
# Игнорирование git проверок (осторожно!)
npm run release -- --force

# Только предварительный просмотр
npm run release:dry
```

## 🚀 Workflow

### Рекомендуемый процесс

1. **Разработка** с conventional commits
2. **Анализ**: `npm run version:analyze`
3. **Релиз**: `npm run release`
4. **Автоматическая** отправка в репозиторий

### Пример полного цикла

```bash
# 1. Разработка функции
git commit -m "feat(theme): добавить поддержку светлой темы"

# 2. Исправление багов
git commit -m "fix(ui): исправить отступы в панели"

# 3. Предварительный анализ
npm run version:analyze
# → Покажет: MINOR релиз (feat найден)

# 4. Автоматический релиз
npm run release
# → Создаст minor версию, запустит тесты, соберет пакет

# 5. Автоматическая отправка (postrelease)
# → Отправит теги в GitHub, создаст VSIX пакет
```

## ⚙️ Конфигурация

### Файлы конфигурации

- `.versionrc.json` - настройки standard-version
- `commitlint.config.js` - правила conventional commits
- `scripts/smart-release.ts` - логика умной системы

### Настройка в package.json

```json
{
  "scripts": {
    "release": "ts-node scripts/smart-release.ts",
    "release:dry": "ts-node scripts/smart-release.ts --dry-run",
    "version:analyze": "ts-node scripts/smart-release.ts --dry-run --force"
  }
}
```

## 🎯 Преимущества

### Для разработчиков

- ✅ **Автоматизация**: не нужно думать о типе релиза
- ✅ **Безопасность**: автоматические проверки
- ✅ **Прозрачность**: детальная отчетность
- ✅ **Гибкость**: можно переопределить автоматику

### Для проекта

- ✅ **Соответствие SemVer**: правильное семантическое версионирование
- ✅ **Качество**: обязательные тесты и валидация
- ✅ **Отслеживаемость**: подробный changelog
- ✅ **Масштабируемость**: подходит для команд любого размера

## 📚 Справочная информация

### Conventional Commits

```
<type>[optional scope]: <description>

[optional body]

[optional footer(s)]
```

### Поддерживаемые типы

- `feat:` - новая функциональность
- `fix:` - исправление бага
- `perf:` - улучшение производительности
- `docs:` - документация
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - тесты
- `build:` - сборка
- `ci:` - CI/CD
- `chore:` - прочее

### Breaking Changes

```bash
# Вариант 1: с восклицательным знаком
git commit -m "feat(api)!: новый формат конфигурации"

# Вариант 2: с BREAKING CHANGE в footer
git commit -m "feat(api): новый формат конфигурации

BREAKING CHANGE: изменен формат файла конфигурации"
```
