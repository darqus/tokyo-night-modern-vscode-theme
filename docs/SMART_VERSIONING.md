# Умная система автоматического версионирования

## 🤖 Обзор

Реализована продвинутая умная система релизов, которая автоматически анализирует conventional commits и определяет правильный тип версии (patch/minor/major) без ручного указания. Система интеллектуально обрабатывает коммиты, предоставляет детальную аналитику и обеспечивает безопасное создание релизов.

## ✨ Ключевые возможности

### 🔍 Интеллектуальный анализ коммитов

- **Автоматический парсинг**: Анализ conventional commits с поддержкой всех стандартных типов
- **Умное определение версии**: Автоматический выбор patch/minor/major на основе семантики изменений
- **Обнаружение Breaking Changes**: Поддержка `feat!:`, `fix!:` и `BREAKING CHANGE:` в теле коммита
- **Анализ области влияния**: Учет scope для более точного определения типа релиза

### 📊 Продвинутая аналитика

- **Детальная статистика**: Подсчет изменений по типам и категориям
- **Умная отчетность**: Краткие и детальные отчеты с эмодзи и цветовым кодированием
- **Предварительный просмотр**: Dry-run режим для безопасной проверки
- **Исторический анализ**: Анализ коммитов с определенного тега или периода

### �️ Безопасность и надежность

- **Автоматические проверки**: Git статус, тесты, сборка перед релизом
- **Force режим**: Возможность обхода проверок для экстренных случаев
- **Откат изменений**: Безопасная работа с git состоянием
- **Валидация**: Проверка качества кода и тем перед релизом

## 📋 Команды

### 🚀 Основные команды (рекомендуется)

```bash
# Умный автоматический релиз - ГЛАВНАЯ КОМАНДА
npm run release

# Только анализ коммитов без создания релиза
npm run version:analyze

# Предварительный просмотр релиза (dry-run)
npm run release:dry

# Умный релиз с детальной аналитикой
npm run version:smart -- --verbose
```

### 🔧 Специальные режимы

```bash
# Принудительный релиз (игнорирует git проверки)
npm run release:force

# Анализ с определенного тега
npm run version:analyze -- --since v1.0.0

# Детальный анализ всех коммитов
npm run version:smart -- --verbose --dry-run
```

### 📦 Совместимые команды (классические)

```bash
npm run release:patch    # Принудительный patch (0.0.X)
npm run release:minor    # Принудительный minor (0.X.0)
npm run release:major    # Принудительный major (X.0.0)
npm run release:alpha    # Альфа версия (1.0.0-alpha.0)
npm run release:beta     # Бета версия (1.0.0-beta.0)
npm run release:rc       # Release candidate (1.0.0-rc.0)
npm run release:first    # Первый релиз проекта
```

## 🧠 Продвинутая логика определения версий

### Интеллектуальные правила приоритета

1. **MAJOR** (X.0.0) - высший приоритет:
   - Любой коммит с breaking change: `feat!:`, `fix!:`, `refactor!:`
   - Наличие `BREAKING CHANGE:` в теле или footer коммита
   - Критические изменения API или поведения

2. **MINOR** (0.X.0) - средний приоритет:
   - Новые функции: `feat:` (без breaking changes)
   - Обратно совместимые улучшения API
   - Новые возможности тем или компонентов

3. **PATCH** (0.0.X) - низший приоритет:
   - Исправления багов: `fix:`
   - Улучшения производительности: `perf:`
   - Внутренние улучшения без изменения API

### Обработка специальных случаев

| Сценарий | Логика | Результат |
|----------|--------|-----------|
| Только `docs:` | Нет изменений кода | PATCH |
| `feat:` + `fix:` | Есть новые функции | MINOR |
| `fix!:` | Breaking fix | MAJOR |
| `perf:` + optimizations | Улучшение производительности | PATCH |
| Empty commits range | Нет коммитов для релиза | Прерывание |

## 🔍 Подробные примеры анализа

### Сценарий 1: Только исправления и документация

```bash
Коммиты:
- fix(theme): исправить контрастность кнопок в темной теме
- fix(build): исправить сборку на Windows системах
- docs(readme): обновить инструкции по установке
- perf(palette): оптимизировать генерацию цветов

Анализ:
✨ Features: ❌ (0)
🐛 Fixes: ✅ (2)
💥 Breaking: ❌ (0)
⚡ Performance: ✅ (1)

Результат: PATCH версия (1.2.3 → 1.2.4)
```

### Сценарий 2: Новые функции с исправлениями

```bash
Коммиты:
- feat(theme): добавить поддержку светлой темы
- feat(variants): новый минимальный вариант темы
- fix(ui): исправить отступы в боковой панели
- docs(api): добавить примеры использования

Анализ:
✨ Features: ✅ (2)
🐛 Fixes: ✅ (1)
💥 Breaking: ❌ (0)
⚡ Performance: ❌ (0)

Результат: MINOR версия (1.2.3 → 1.3.0)
```

### Сценарий 3: Breaking changes

```bash
Коммиты:
- feat(api)!: новый формат конфигурационных файлов
- refactor(theme): переработать структуру цветов

BREAKING CHANGE: Изменен формат файлов конфигурации.
Старые файлы нуждаются в миграции.

- fix(build): исправить совместимость с новым API

Анализ:
✨ Features: ✅ (1)
🐛 Fixes: ✅ (1)
💥 Breaking: ✅ (1)
⚡ Performance: ❌ (0)

Результат: MAJOR версия (1.2.3 → 2.0.0)
```

## 📊 Подробный вывод системы

### Краткий режим (по умолчанию)

```bash
🤖 Запускаем умную систему релизов...

🔍 Анализ коммитов завершен:
   📝 Коммитов для анализа: 8
   ✨ Features: ✅
   🐛 Fixes: ✅
   💥 Breaking: ❌
   ⚡ Performance: ✅

📈 Рекомендованный тип релиза: MINOR

📋 Краткое содержание изменений:
   • ✨ добавить поддержку светлой темы
   • ✨ новый минимальный вариант темы
   • 🐛 исправить отступы в боковой панели
   • ⚡ оптимизировать генерацию цветов
   • 📚 обновить документацию API

📊 Версия: 1.2.3 → 1.3.0

📊 Информация о релизе:
   🏷️  Текущая версия: 1.2.3
   🏷️  Новая версия: 1.3.0
   📝 Изменений: 8
   📈 Тип релиза: MINOR

🏗️  Выполняем релиз...
🧪 Запускаем тесты...
🏗️  Собираем проект...
📦 Создаем minor релиз...
📦 Создаем VSIX пакет...

✅ Умный релиз успешно завершен!
```

### Детальный режим (--verbose)

```bash
🔍 Анализ коммитов для определения версии...

📊 Детальная статистика коммитов:
   Всего коммитов: 8
   Features (feat): 2
   Fixes (fix): 2
   Performance (perf): 1
   Breaking changes: 0
   Прочие: 3

� Все коммиты:
   a1b2c3d feat(theme): добавить поддержку светлой темы
   e4f5g6h feat(variants): новый минимальный вариант темы
   i7j8k9l fix(ui): исправить отступы в боковой панели
   m0n1o2p fix(build): исправить сборку на Windows
   q3r4s5t perf(palette): оптимизировать генерацию цветов
   u6v7w8x docs(readme): обновить инструкции
   y9z0a1b style(code): улучшить форматирование
   c2d3e4f test(smoke): добавить smoke тесты

🔍 Анализ коммитов завершен:
   📝 Коммитов для анализа: 8
   ✨ Features: ✅
   🐛 Fixes: ✅
   💥 Breaking: ❌
   ⚡ Performance: ✅

📈 Рекомендованный тип релиза: MINOR

📋 Краткое содержание изменений:
   • ✨ добавить поддержку светлой темы
   • ✨ новый минимальный вариант темы
   • 🐛 исправить отступы в боковой панели
   • 🐛 исправить сборку на Windows
   • ⚡ оптимизировать генерацию цветов
   • 📚 обновить инструкции
   • 💄 улучшить форматирование
   • 🧪 добавить smoke тесты

📊 Версия: 1.2.3 → 1.3.0
```

### Режим только анализа

```bash
� Анализ коммитов для определения версии...

🔍 Анализ коммитов завершен:
   📝 Коммитов для анализа: 5
   ✨ Features: ✅
   � Fixes: ❌
   💥 Breaking: ❌
   ⚡ Performance: ❌

📈 Рекомендованный тип релиза: MINOR

📋 Краткое содержание изменений:
   • ✨ добавить темный режим
   • ✨ новые цветовые схемы
   • 📚 обновить документацию
   • 🔧 настроить CI/CD
   • 💄 улучшить стили кода

📊 Версия: 1.2.3 → 1.3.0

# Релиз НЕ создан - только анализ
```

## 🛡️ Система безопасности и проверок

### Автоматические проверки (по умолчанию)

1. **Git состояние**:

   ```bash
   ⚠️  Есть незакоммиченные изменения:
   M  src/palette.ts
   ?? temp-file.txt
   ```

2. **Запуск тестов**:

   ```bash
   🧪 Запускаем тесты...
   ✅ Все тесты прошли успешно
   ```

3. **Сборка проекта**:

   ```bash
   🏗️  Собираем проект...
   ✅ Сборка завершена успешно
   ```

4. **Валидация тем**:

   ```bash
   🔍 Валидация тем...
   ✅ Все темы прошли валидацию
   ```

### Режим обхода проверок (--force)

```bash
npm run release:force

⚠️  ВНИМАНИЕ: Режим принудительного релиза!
⚠️  Git проверки пропущены
🔍 Анализ коммитов...
# ... продолжение релиза
```

### Предупреждения о Breaking Changes

```bash
📊 Информация о релизе:
   🏷️  Версия: 2.0.0
   📝 Изменений: 12
   💥 ВНИМАНИЕ: Есть breaking changes!

⚠️  Breaking changes обнаружены:
   • feat(api)!: новый формат конфигурации
   • refactor(theme)!: изменена структура цветов

🚨 Этот релиз может сломать совместимость!
```

## 🚀 Рекомендуемый Workflow

### Полный цикл разработки

```bash
# 1. Разработка с правильными коммитами
git add .
git commit -m "feat(theme): добавить поддержку светлой темы"

git add .
git commit -m "fix(ui): исправить контрастность кнопок"

git add .
git commit -m "perf(build): оптимизировать время сборки"

# 2. Предварительный анализ (опционально)
npm run version:analyze
# Покажет: MINOR релиз из-за feat

# 3. Умный релиз
npm run release
# Автоматически создаст MINOR версию

# 4. Отправка в репозиторий
git push --follow-tags origin main

# 5. Публикация (опционально)
npm run publish
```

### Экстренные исправления

```bash
# 1. Быстрое исправление
git commit -m "fix(critical): исправить критическую уязвимость"

# 2. Экстренный релиз с обходом проверок
npm run release:force

# 3. Немедленная отправка
git push --follow-tags origin main
```

### Работа с feature branch

```bash
# В feature branch
git commit -m "feat(api): добавить новый эндпоинт"
git commit -m "test(api): добавить тесты для эндпоинта"

# Перед мержем в main - проверка
npm run version:analyze
# Покажет какой релиз будет создан

# После мержа в main
git checkout main
git pull
npm run release  # Создаст релиз с анализом всех новых коммитов
```

## ⚙️ Конфигурация и настройка

### Файлы конфигурации

1. **`.versionrc.json`** - Настройки standard-version:

   ```json
   {
     "types": [
       { "type": "feat", "section": "✨ Новые возможности" },
       { "type": "fix", "section": "🐛 Исправления" }
     ],
     "scripts": {
       "postrelease": "echo '🚀 Релиз завершен!'"
     }
   }
   ```

2. **`commitlint.config.js`** - Правила conventional commits:

   ```javascript
   module.exports = {
     extends: ['@commitlint/config-conventional'],
     rules: {
       'type-enum': [2, 'always', ['feat', 'fix', 'perf']]
     }
   }
   ```

3. **`scripts/smart-version.ts`** - Основная логика умной системы

### Настройка в package.json

```json
{
  "scripts": {
    "release": "ts-node scripts/smart-version.ts",
    "release:dry": "ts-node scripts/smart-version.ts --dry-run",
    "version:analyze": "ts-node scripts/smart-version.ts --dry-run --force",
    "version:smart": "ts-node scripts/smart-version.ts"
  }
}
```

## 🎯 Преимущества умной системы

### Для разработчиков

- ✅ **Полная автоматизация**: Не нужно думать о типе релиза
- ✅ **Интеллектуальность**: Система понимает семантику изменений
- ✅ **Безопасность**: Множественные проверки и валидация
- ✅ **Прозрачность**: Детальная отчетность на каждом этапе
- ✅ **Гибкость**: Можно переопределить любое автоматическое решение
- ✅ **Удобство**: Простые команды для сложных операций

### Для проекта

- ✅ **Соответствие SemVer**: Точное семантическое версионирование
- ✅ **Качество релизов**: Обязательные тесты и валидация
- ✅ **Отслеживаемость**: Подробный changelog с эмодзи
- ✅ **Командная работа**: Подходит для любого размера команды
- ✅ **CI/CD интеграция**: Легко интегрируется в пайплайны
- ✅ **Профессионализм**: Согласованность и стандартизация

### Для пользователей

- ✅ **Понятные релизы**: Четкое разделение по типам изменений
- ✅ **Безопасность обновлений**: Предсказуемость breaking changes
- ✅ **Качество**: Каждый релиз проходит полную проверку
- ✅ **Документированность**: Подробный changelog на русском языке

## 📚 Справочная информация

### Conventional Commits Reference

```bash
# Основной формат
<type>[optional scope]: <description>

[optional body]

[optional footer(s)]

# Примеры
feat(theme): добавить темную тему
fix(ui): исправить отступы кнопок
feat(api)!: новый формат конфигурации
perf(build): ускорить сборку на 30%

# Breaking change в footer
feat(api): новый метод аутентификации

BREAKING CHANGE: изменен интерфейс API аутентификации
```

### Поддерживаемые типы коммитов

| Тип | Описание | Влияние на версию | Секция в changelog |
|-----|----------|-------------------|-------------------|
| `feat:` | Новая функциональность | MINOR | ✨ Новые возможности |
| `fix:` | Исправление бага | PATCH | 🐛 Исправления |
| `perf:` | Улучшение производительности | PATCH | ⚡ Улучшения производительности |
| `revert:` | Откат изменений | Зависит от отката | ⏪ Откаты |
| `docs:` | Документация | PATCH | 📚 Документация |
| `style:` | Форматирование кода | PATCH | 💄 Стили и форматирование |
| `refactor:` | Рефакторинг | PATCH | ♻️ Рефакторинг |
| `test:` | Тесты | PATCH | 🧪 Тесты |
| `build:` | Сборка и зависимости | PATCH | 🏗️ Сборка и зависимости |
| `ci:` | CI/CD | PATCH | 👷 CI/CD |
| `chore:` | Прочие изменения | PATCH | Скрыто |

### Breaking Changes Syntax

```bash
# Вариант 1: Восклицательный знак в заголовке
feat(api)!: новый формат конфигурации
fix(theme)!: исправить критическую ошибку цветов
refactor(build)!: переработать систему сборки

# Вариант 2: BREAKING CHANGE в footer
feat(api): добавить аутентификацию через OAuth

BREAKING CHANGE: изменен интерфейс API.
Требуется обновление клиентского кода.

# Вариант 3: BREAKING-CHANGE (альтернативный синтаксис)
feat(config): новый формат файлов

BREAKING-CHANGE: старые конфигурационные файлы
больше не поддерживаются.
```

### Командная справка

```bash
# Показать справку
npm run release -- --help
npm run version:smart -- --help

# Основные команды
npm run release                    # Умный релиз
npm run release:dry               # Предварительный просмотр
npm run version:analyze           # Только анализ коммитов
npm run release:force             # Принудительный релиз

# Опции
--dry-run                         # Режим предварительного просмотра
--force                          # Игнорировать git проверки
--verbose                        # Детальный вывод
--since <tag>                    # Анализ с определенного тега

# Примеры с опциями
npm run release -- --verbose     # Релиз с детальной информацией
npm run version:analyze -- --since v1.0.0  # Анализ с версии 1.0.0
npm run release -- --dry-run --verbose     # Детальный предпросмотр
```

## 🔧 Устранение неполадок

### Частые проблемы

1. **"Нет коммитов для релиза"**:

   ```bash
   ℹ️  Нет коммитов для релиза с последней версии

   # Решение: убедитесь что есть новые коммиты с последнего тега
   git log --oneline $(git describe --tags --abbrev=0)..HEAD
   ```

2. **"Незакоммиченные изменения"**:

   ```bash
   ⚠️  Есть незакоммиченные изменения:
   M  src/theme.ts

   # Решение: закоммитьте или используйте --force
   git add . && git commit -m "fix: завершить изменения"
   # или
   npm run release:force
   ```

3. **"Тесты не прошли"**:

   ```bash
   ❌ Тесты не прошли

   # Решение: исправьте тесты или используйте --force
   npm run test  # проверить какие тесты падают
   ```

### Отладка

```bash
# Подробный анализ
npm run version:smart -- --verbose --dry-run

# Проверка git статуса
git status

# Проверка последних коммитов
git log --oneline -10

# Проверка тегов
git tag -l

# Ручная проверка стандартной версии
npx standard-version --dry-run
```
