# 🎨 Универсальная Цветовая Палитра Tokyo Modern

Этот документ описывает новую универсальную цветовую палитру, разработанную для темы Tokyo Modern. Она обеспечивает более гибкую и структурированную систему цветов по сравнению с предыдущей версией.

## 🏗️ Структура Универсальной Палитры

Новая палитра организована в следующие категории:

### 1. Фоновые Цвета (`background`)

- `base`: Основной фон
- `elevated`: Поднятые элементы (панели, карточки)
- `inset`: Внутренние элементы
- `overlay`: Оверлеи, модальные окна

### 2. Цвета Текста (`foreground`)

- `primary`: Основной текст
- `secondary`: Второстепенный текст
- `muted`: Приглушенный текст
- `disabled`: Текст отключенных элементов

### 3. Семантические Цвета (`semantic`)

- `success`: Цвета успеха/позитивных действий
- `warning`: Цвета предупреждений
- `error`: Цвета ошибок
- `info`: Информационные цвета
- `accent`: Акцентные цвета

### 4. Хроматические Цвета (`chromatic`)

- `blue`, `cyan`, `teal`, `green`, `purple`, `orange`, `yellow`, `red`, `pink`, `neutral`, `indigo`, `lime`, `magenta`, `rose`, `amber`, `emerald`

### 5. UI Цвета (`ui`)

- `white`, `black`, `badge`: Базовые UI-цвета
- `border`, `scrollbar`, `selection`: Цвета элементов интерфейса

## 🔄 Обратная Совместимость

Для обеспечения обратной совместимости сохранена старая структура палитры, а также добавлена совместимая палитра, которая мапит старые цвета на новые.

## 🚀 Использование по Умолчанию

Начиная с текущей версии, универсальная палитра используется по умолчанию для генерации темы. Генератор темы теперь использует `generateFromUniversalConfig` вместо `generateFromConfig`, что обеспечивает использование новой структуры палитры во всей теме.

## 🛠️ Архитектура Генерации

- `generateFromUniversalConfig` - основная функция генерации темы, использует универсальную палитру
- `generateFromConfig` - сохранена для обратной совместимости, использует старую палитру
- `generateUIColorsWithUniversal`, `generateTokenColorsWithUniversal`, `generateSemanticTokensWithUniversal` - функции для генерации различных компонентов темы с использованием универсальной палитры

## � Улучшения по Сравнению с Предыдущей Версией

1. **Более логичная структура**: Цвета сгруппированы по функциональному назначению
2. **Расширенные варианты**: Каждая категория цвета имеет больше градаций (от darkest до lightest)
3. **Семантические цвета**: Добавлены цвета с определенным значением (success, warning и т.д.)
4. **Лучшая масштабируемость**: Новая структура позволяет легко добавлять новые цвета и категории
5. **Улучшенная согласованность**: Более последовательные отношения между оттенками

## 🎯 Использование

Для использования новой палитры импортируйте:

```typescript
import { universalPalette } from '../theme/palette/index.js'
```

Для обратной совместимости:

```typescript
import { compatiblePalette } from '../theme/palette/index.js'
```

## 🧪 Тестирование

Новая палитра была протестирована на соблюдение логической прогрессии яркости и контрастности. Все цвета следуют ожидаемой прогрессии от темного к светлому в своих соответствующих категориях.
