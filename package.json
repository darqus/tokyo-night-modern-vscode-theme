{
  "name": "tokyo-modern",
  "displayName": "Tokyo Modern",
  "description": "Beautiful modern dark theme for VS Code with carefully crafted colors and contemporary design",
  "version": "2.3.2378",
  "author": "lod",
  "publisher": "lod-inc",
  "license": "MIT",
  "type": "module",
  "engines": {
    "vscode": "^1.74.0"
  },
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/darqus/tokyo-modern-vscode-theme"
  },
  "bugs": {
    "url": "https://github.com/darqus/tokyo-modern-vscode-theme/issues"
  },
  "categories": [
    "Themes"
  ],
  "keywords": [
    "theme",
    "tokyo",
    "night",
    "dark"
  ],
  "contributes": {
    "themes": [
      {
        "label": "Tokyo Modern",
        "uiTheme": "vs-dark",
        "path": "./themes/tokyo-modern-color-theme.json"
      }
    ]
  },
  "scripts": {
    "build": "node --loader ts-node/esm --no-warnings scripts/build-theme.ts && biome format --write themes/tokyo-modern-color-theme.json",
    "build:vsix": "node --loader ts-node/esm --no-warnings scripts/build-vsix.ts",
    "package": "npm run build:vsix",
    "release": "node --loader ts-node/esm --no-warnings scripts/release.ts",
    "lint": "biome lint .",
    "format": "biome check --fix src/ scripts/ && biome format --write src/ scripts/",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "check:contrast": "node --loader ts-node/esm --no-warnings scripts/check-contrast.ts",
    "check:syntax": "node --loader ts-node/esm --no-warnings scripts/check-syntax-contrast.ts",
    "analyze:colors": "node --loader ts-node/esm --no-warnings scripts/analyze-token-colors.ts",
    "deps:check": "ncu",
    "deps:update": "ncu -u",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.3",
    "@types/node": "^24.10.0",
    "@vitest/coverage-v8": "^4.0.6",
    "@vitest/ui": "^4.0.6",
    "@vscode/vsce": "^3.6.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6",
    "npm-check-updates": "^19.1.2",
    "standard-version": "^9.5.0",
    "ts-node": "^10.9.2",
    "tslib": "^2.8.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.6"
  }
}
